{
  "name": "communiconnect",
  "version": "1.0.0",
  "description": "Plateforme de communication locale pour la Guinée",
  "private": true,
  "scripts": {
    "start": "cd frontend && npm start",
    "start:backend": "cd backend && python manage.py runserver",
    "start:dev": "concurrently \"npm run start:backend\" \"npm run start\"",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && pip install -r requirements.txt",
    "build": "cd frontend && npm run build",
    "test": "cd frontend && npm test",
    "lint": "cd frontend && npm run lint"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/votre-username/communiconnect.git"
  },
  "keywords": [
    "react",
    "django",
    "guinea",
    "social-network",
    "local-communication"
  ],
  "author": "Votre Nom",
  "license": "MIT",
  "dependencies": {
    "@heroicons/react": "^2.2.0",
    "axios": "^1.10.0"
  }
}
